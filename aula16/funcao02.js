function soma(n1 = 0, n2 = 0) { //definindo os 2 números como 0, caso um deles não seja preenchido ainda teremos retorno numérico
        return n1 + n2 // retornando a soma de 2 números
}

console.log(soma(2, 5)) // escrevendo a função
// se não forem definidos 2 números o retorno será "NaN"  → not a number